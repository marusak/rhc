---
- name: Connect to remote Web console webserver
  hosts: "localhost"
  become: true
  tasks:
    - name: Connect to remote Web console webserver
      shell: nohup /usr/lib/client/start </dev/null >/dev/null 2>&1 &
